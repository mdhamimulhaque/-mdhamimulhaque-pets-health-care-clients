import React from 'react';
import useTitle from '../../hooks/useTitle';

const Error = () => {
    useTitle('Error')
    return (
        <div>
            error
        </div>
    );
};

export default Error;